/**
 * Modal Component Barrel Export
 * 
 * Central export point for the Modal component and its types.
 */

export { Modal, default } from './Modal';
export type { ModalProps, ModalSize } from './Modal.types';
